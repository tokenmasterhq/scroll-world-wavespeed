# Copy to .env and fill in your key, or run:
#   python3 references/bootstrap.py
# Never commit .env. Used by references/pipeline.md's WaveSpeed backend:
# `set -a; source .env; set +a` before running the gen_*_ws functions.

WAVESPEED_API_KEY=your_wavespeed_api_key_here
